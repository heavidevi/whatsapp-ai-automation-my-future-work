# Session Notes — 2026-04-23

Dev session log for the humanize pass, Phase 8-11, in-bot upsell, and bug fixes from live testing. Most recent commits first.

## Phases landed today

### Phase 8 — Objection handler (already committed earlier in session)
Commit `0860992` — humanize pass: gentler sales objections, don't switch language on names.

- New [src/conversation/handlers/objectionHandler.js](src/conversation/handlers/objectionHandler.js)
- SALES_CHAT objection interceptor in [router.js](src/conversation/router.js) — regex fast-path + LLM fallback
- Softened Stage 6 in the sales prompt (removed tier-drop mandate, killed fake urgency)
- Guardrails in all three "Language Rules" prompt blocks — names and proper nouns are NOT language signals
- Localizer safety net when current message is English

### Phase 9 — Session recap after inactivity
Commit `39a3840` — session recap: warm welcome-back after 30 min of silence.

- New [src/conversation/sessionRecap.js](src/conversation/sessionRecap.js)
- Fires only when gap > 30 min AND there's real context to reference
- LLM-composed 1-2 sentence recap; banned "continue where we left off"
- Skips on slash commands, button taps, fresh users, transient states

### Phase 10 — Interactive reply matcher
Commit `0c1e1f1` — type a number or the option name to pick.

- New [src/messages/interactiveReplyMatcher.js](src/messages/interactiveReplyMatcher.js)
- Appends "Or type 1, 2, or 3" hint to every outgoing interactive
- Fast-path digits + exact titles (0 LLM cost); LLM fallback for fuzzy phrasings
- Returns `{kind, item, total}` — `out_of_range` preserves pending for retry
- New [src/boot/handlerSmokeCheck.js](src/boot/handlerSmokeCheck.js) — catches missing-import bugs at boot
- Fixed latent `sendCTAButton` missing-import bug in serviceSelection.js (hit when user asked about online store)

### Phase 11 — Cross-flow entity carryover
Commit `7002857` — phase 11: cross-flow entity carryover + smarter menu routing.

- New `getSharedBusinessContext(user)` in [entityAccumulator.js](src/conversation/entityAccumulator.js)
- `startAdFlow`, `startLogoFlow`, `startChatbotFlow` — each pre-fills shared fields and jumps to first missing state
- Intent classifier now recognizes "forget X, do Y" as `menu` intent
- `pickServiceFromSwitch` in serviceSelection.js — LLM-backed service picker for negated/ambiguous flow-switches
- `/menu` sends a dedicated main-menu greeting via `sendMainMenu` (no more "didn't catch that" for /menu itself)
- Exploratory question check moved ABOVE matchServiceFromText so "any other services?" pre-empts the /what/ FAQ match
- `/reset` expanded to clear `conversationSummary`, domain state, `preferredLanguage`, `objectionTopics`, upsell flags, lead counters, and other lingering fields
- Industry extractor prompt strengthened so business-name inference on delegation is a first-class move ("Hasnain Plumbing" + "pick one for me" → "Plumbing")

### In-bot post-website upsell (outside the phase plan)
Commit `355e090` — post-website upsell: one soft in-bot pitch after site goes live.

- [postPayment.js](src/payments/postPayment.js) — one message right after domain is registered
- Picks the most useful next service the user hasn't touched: SEO audit → logo → chatbot → skip
- Idempotent via `metadata.postWebsiteUpsellSent`, low-pressure wording ("say X whenever")
- Complements the existing email upsell (day 7/30/60/90)

### Bug fixes from live testing
Commit `d86e741` — webdev + localizer: parse multi-field contact blobs, kill stale language caches.

- Multi-field contact: "email is X and phone is Y" used to store Y as the email. `labelCount` regex now counts "label is"/"label are", not just ":" / "-". Multi-field input now goes through `parseContactFields` which extracts each field with its own regex.
- Localizer: `resolveLanguage` now auto-fetches the latest user message from conversations when no `latestUserMessage` is passed. Every `localize()` caller benefits from the English-override safety net without threading the current message through every summary / peek / ack callsite.

## Other small fixes bundled in

- `seo audit: tighten llm + scraper timeouts so stalls surface` — commit `d327d3a`
- `hvac template: add 8 more trades (electrical, roofing, appliance, garage door, locksmith, pest control, water damage, tree service)` — commit `bc35564`
- `webdev summary: preview trade defaults instead of "None (skipped)"` — commit `da18de0`

---

## Testing still outstanding

Test each of these on real WhatsApp. Log lines to watch are in parentheses.

### Phase 11 — cross-flow carryover

- [ ] `/reset` → complete a webdev flow end-to-end with business name + industry (e.g. "plumbing business in Austin") → tap **Logo Maker** from `/menu`. Expect: greeting references the business name + industry, jumps straight to "what does your business do in one sentence?" (no re-asking name or industry).
- [ ] Same setup → tap **Marketing Ads**. Expect: jumps to "what product is this ad promoting?".
- [ ] Same setup → tap **AI Chatbot**. Expect: greeting mentions services carried over + jumps to FAQs.
- [ ] Fresh user (`/reset`, no webdev data): tap Logo Maker. Expect: standard "what is your business name?" greeting (no shared data to carry).
- [ ] Partial carryover: start webdev, give only the business name, bail before industry. `/menu` → Logo Maker. Expect: greeting references name, asks industry.

Log lines: `Started logo flow with prefilled name=...`, `Started ad generation with prefilled name=...`, `Started chatbot flow with prefilled name=..., industry=..., services=true`.

### Flow-switch mid-conversation

- [ ] Mid-webdev (at contact step): send "forget the website, can you do ai chatbot for me?". Expect: ONE message — chatbot flow starts (no webdev re-prompt).
- [ ] Mid-ad-flow: "scrap this, lets do a logo". Expect: logo flow starts.
- [ ] Mid-any: "wait can you also build a chatbot?". Expect: chatbot flow starts.
- [ ] Negative control: "how much does it cost?" mid-webdev. Expect: LLM aside + "back to where we were" (off-topic question, not a flow-switch).

Log lines: `[INTERACTIVE]`, `[ROUTER]` classify-intent → menu.

### `/menu` command

- [ ] `/reset` → `/menu`. Expect: *"Here's what I can help with — pick one to get started:"* with 3 buttons. NOT "hmm, didn't catch that".
- [ ] Mid any-flow: `/menu`. Same expected behavior.
- [ ] "any other services?" while in SERVICE_SELECTION. Expect: full 10-item service list (svc_more), not the 3-button menu.

### Industry extraction (business-name inference)

- [ ] Start webdev with business name containing a trade word — "hasnain plumbing", "maria's thai kitchen", "bright dental". When asked industry, reply "i'm not sure, pick one based on my business name". Expect: extractor returns the right industry from the name (Plumbing / Thai Restaurant / Dental), flow continues.

Log line: `Got it, I'll go with *<industry>*`.

### Multi-field contact input

- [ ] Get to WEB_COLLECT_CONTACT. Send *"email is test@gmail.com and phone is 09876544567"*. Expect: contact summary shows email AND phone stored separately, no "updated email to 09876544567" mixup.
- [ ] Variations: *"my email is X, my phone is Y, address is Z"*, *"foo@bar.com, 555-1234, 123 Main St"*.

### Localizer safety net (English-override)

- [ ] Set up a user who somehow has `preferredLanguage = 'roman-urdu'` cached. In WhatsApp, send a clearly English message. Expect: bot reply is English, not translated.
- [ ] Send a Roman Urdu message ("mujhe website chahiye"). Expect: bot reply is in Roman Urdu (normal multilingual path still works).

### Post-website in-bot upsell

- [ ] Complete a full webdev flow → accept domain → wait for "domain registered and configured" message. Expect: a follow-up pitch message ~3-4 seconds later ("say audit and i'll run a free SEO check...").
- [ ] Re-run payment webhook (simulate retry). Expect: NO second pitch (idempotent via `postWebsiteUpsellSent`).
- [ ] Reply "audit" after the pitch. Expect: SEO audit flow starts.

Log line: `[PAY] Site-live upsell pitched (seo_audit) to +phone`.

### `/reset` scope

- [ ] Build a webdev site end-to-end (with domain purchased). `/reset`. Send *"can you do a website for me please"*. Expect: fresh flow starts with standard "what's your business name?" — no reference to previous business name, industry, or domain suggestion.
- [ ] After /reset: `adSource` / `paymentConfirmed` / `adReferral` should still be in `metadata` (intentionally kept for attribution).

### Phase 10 — interactive reply matcher

- [ ] `/menu` → send `5` (out of range). Expect: *"that was only 3 options — pick 1-3 or tap one of the buttons above."* then menu buttons still work — type `2` and it resolves to button 2.
- [ ] `/menu` → send `the second one please`. Expect: 2nd button fires.
- [ ] `/menu` → send `website`. Expect: webdev flow starts via keyword match.
- [ ] Mid-flow (answering "how many years?"): type `5`. Expect: treated as the answer, NOT a button pick.

### Phase 9 — session recap

- [ ] Start a webdev flow, give name + industry, let it sit. Wait 31+ minutes. Send "hi". Expect: *"welcome back — we were working on your Glow Studio site. i had your name + industry. next up is services."* before the handler's reply.
- [ ] After 31 min, send `/reset`. Expect: NO recap (slash command skips).
- [ ] Fresh user, 31 min idle, send "hi". Expect: NO recap (no accumulated context).

### Sales-chat objection handling (Phase 8)

- [ ] After SEO audit + sales pitch, reply "its too expensive, i'll look at alternatives". Expect: one-line soft acknowledge, no tier drop, no bullet list.
- [ ] "let me think about it". Expect: *"of course, take your time. ping me whenever."* and silence.
- [ ] "what's the price for the Pro package?". Expect: normal sales-bot price answer (NOT objection handler — it's a question).

Log line: `[SALES] Objection intercepted for +phone`.

---

## Deferred / not yet built

### Phases 12-15 (still remaining)
- **Phase 12** — Multi-service queue ("I need a website AND a logo AND some ads" in one message → acknowledge all, queue, sequence)
- **Phase 13** — Abuse detection (firm polite decline for hate / phishing / "help me hack a site")
- **Phase 14** — Document + location handling (webhook parser for `document` and `location`, reverse-geocode into address)
- **Phase 15** — Return-visitor recognition (specific greeting referencing completed projects)

### Known pre-existing issues not touched today
- Stale `via_phone_number_id` on some user rows causing "Unsupported request - method type: post" errors from Meta on follow-up sends. One-time audit + backfill script would clean the DB. Can also add a sender-level fallback that retries with `env.whatsapp.phoneNumberId` on error 100.
- 34-minute gap some users saw between "⏳ Analyzing..." and the audit report — not reproducible from code, most likely Meta webhook / network queuing on the user's side. Timeouts were tightened so stalls surface as errors rather than silent hangs.

### Outside scope of the phase plan
- Full cross-sell engine (Option C from earlier discussion) — current implementation is Option B (one pitch at site-live). If the single pitch converts well, we can expand to multi-event delivery-time cross-sell.
